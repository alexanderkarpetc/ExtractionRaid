/* Quest popup — UI Toolkit port of Assets/Concepts/quest-ui-popup.html.
 * Self-contained colours (no token dependency) so the visual matches the
 * concept exactly; mirrors the approach in UI/Dialogue/NpcDialogue.uss.
 * (rev2 — re-saved to force a clean reimport after metas were generated.) */

.qp-root {
    flex-grow: 1;
    background-color: rgba(0, 0, 0, 0);
}

/* ── Overlay / backdrop ──────────────────────────────── */
.qp-overlay {
    position: absolute;
    left: 0; right: 0; top: 0; bottom: 0;
    background-color: rgba(4, 6, 12, 0.72);
    align-items: center;
    justify-content: center;
}

/* ── Popup window ────────────────────────────────────── */
.qp-popup {
    width: 820px;
    max-width: 96%;
    max-height: 86%;
    background-color: rgba(10, 13, 20, 0.98);
    border-left-width: 1px;
    border-right-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
    border-color: rgba(255, 255, 255, 0.08);
    border-radius: 12px;
    overflow: hidden;
    flex-direction: column;
}

/* ── Header ──────────────────────────────────────────── */
.qp-header {
    padding-top: 18px;
    padding-left: 22px;
    padding-right: 22px;
    border-bottom-width: 1px;
    border-bottom-color: rgb(26, 32, 48);
}

.qp-header-top {
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 14px;
}

.qp-title-area {
    flex-direction: row;
    align-items: center;
}

.qp-title {
    font-size: 16px;
    -unity-font-style: bold;
    letter-spacing: 3px;
    color: rgb(255, 255, 255);
}

/* NPC pill (NPC mode only) */
.qp-npc-pill {
    flex-direction: row;
    align-items: center;
    margin-left: 14px;
    padding-top: 5px;
    padding-bottom: 5px;
    padding-left: 7px;
    padding-right: 10px;
    background-color: rgb(22, 27, 40);
    border-left-width: 1px;
    border-right-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
    border-color: rgb(36, 46, 66);
    border-radius: 6px;
}

.qp-npc-avatar {
    width: 24px;
    height: 24px;
    margin-right: 8px;
    border-radius: 5px;
    background-color: rgb(28, 34, 53);
    border-left-width: 1px;
    border-right-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
    border-color: rgb(36, 46, 66);
}

.qp-npc-pill-name {
    font-size: 12px;
    -unity-font-style: bold;
    color: rgb(224, 228, 236);
}

.qp-npc-pill-role {
    font-size: 9px;
    letter-spacing: 1px;
    margin-left: 8px;
    color: rgb(136, 146, 166);
}

.qp-close {
    width: 28px;
    height: 28px;
    border-radius: 6px;
    background-color: rgb(22, 27, 40);
    border-left-width: 1px;
    border-right-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
    border-color: rgb(36, 46, 66);
    color: rgb(136, 146, 166);
    font-size: 13px;
}

.qp-close:hover {
    border-color: rgb(46, 58, 82);
    color: rgb(224, 228, 236);
}

/* ── Tabs ────────────────────────────────────────────── */
.qp-tabs {
    flex-direction: row;
}

.qp-tab {
    padding-top: 9px;
    padding-bottom: 10px;
    padding-left: 22px;
    padding-right: 22px;
    margin-left: 0;
    margin-right: 2px;
    background-color: rgba(0, 0, 0, 0);
    border-left-width: 0;
    border-right-width: 0;
    border-top-width: 0;
    border-bottom-width: 2px;
    border-bottom-color: rgba(0, 0, 0, 0);
    border-radius: 0;
    font-size: 11px;
    letter-spacing: 1px;
    color: rgb(136, 146, 166);
}

.qp-tab:hover {
    color: rgb(224, 228, 236);
}

.qp-tab--active {
    color: rgb(255, 255, 255);
    border-bottom-color: rgb(74, 124, 255);
}

/* ── Body (scroll) ───────────────────────────────────── */
.qp-body {
    flex-grow: 1;
    padding-top: 18px;
    padding-left: 22px;
    padding-right: 22px;
    padding-bottom: 22px;
}

/* ── Quest card ──────────────────────────────────────── */
.qp-card {
    margin-bottom: 10px;
    background-color: rgb(17, 21, 32);
    border-left-width: 1px;
    border-right-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
    border-color: rgb(36, 46, 66);
    border-radius: 8px;
    overflow: hidden;
}

.qp-card--active {
    border-color: rgba(74, 124, 255, 0.3);
}

.qp-card-hdr {
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    padding-top: 11px;
    padding-bottom: 10px;
    padding-left: 14px;
    padding-right: 14px;
}

.qp-card--open .qp-card-hdr {
    border-bottom-width: 1px;
    border-bottom-color: rgb(36, 46, 66);
}

.qp-card-hdr-left {
    flex-direction: row;
    align-items: center;
}

.qp-card-hdr-right {
    flex-direction: row;
    align-items: center;
}

.qp-icon {
    width: 28px;
    height: 28px;
    margin-right: 10px;
    border-radius: 6px;
    border-left-width: 1px;
    border-right-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
}

.qp-icon--available {
    background-color: rgba(212, 160, 23, 0.12);
    border-color: rgba(212, 160, 23, 0.22);
}

.qp-icon--active {
    background-color: rgba(74, 124, 255, 0.12);
    border-color: rgba(74, 124, 255, 0.22);
}

.qp-icon--done {
    background-color: rgba(46, 204, 113, 0.10);
    border-color: rgba(46, 204, 113, 0.2);
}

.qp-name {
    font-size: 13px;
    -unity-font-style: bold;
    color: rgb(255, 255, 255);
}

.qp-badge {
    font-size: 9px;
    letter-spacing: 1px;
    margin-left: 8px;
    padding-top: 3px;
    padding-bottom: 3px;
    padding-left: 8px;
    padding-right: 8px;
    border-radius: 4px;
    border-left-width: 1px;
    border-right-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
}

.qp-badge--new {
    background-color: rgba(212, 160, 23, 0.12);
    color: rgb(212, 160, 23);
    border-color: rgba(212, 160, 23, 0.2);
}

.qp-badge--active {
    background-color: rgba(74, 124, 255, 0.12);
    color: rgb(74, 124, 255);
    border-color: rgba(74, 124, 255, 0.2);
}

.qp-badge--done {
    background-color: rgba(46, 204, 113, 0.10);
    color: rgb(46, 204, 113);
    border-color: rgba(46, 204, 113, 0.18);
}

.qp-chevron {
    margin-left: 8px;
    font-size: 10px;
    color: rgb(136, 146, 166);
}

.qp-npc-tag {
    flex-direction: row;
    align-items: center;
    font-size: 9px;
    letter-spacing: 1px;
    color: rgb(136, 146, 166);
}

.qp-npc-dot {
    width: 5px;
    height: 5px;
    margin-right: 5px;
    border-radius: 3px;
    background-color: rgba(74, 124, 255, 0.7);
}

/* ── Card body ───────────────────────────────────────── */
.qp-card-body {
    padding-top: 13px;
    padding-bottom: 15px;
    padding-left: 14px;
    padding-right: 14px;
}

.qp-desc {
    font-size: 12px;
    margin-bottom: 10px;
    color: rgb(176, 184, 200);
    white-space: normal;
}

/* Objectives */
.qp-obj-list {
    flex-direction: column;
}

.qp-obj-row {
    flex-direction: row;
    align-items: center;
    margin-bottom: 5px;
    padding-top: 6px;
    padding-bottom: 6px;
    padding-left: 10px;
    padding-right: 10px;
    background-color: rgb(22, 27, 40);
    border-radius: 5px;
    border-left-width: 1px;
    border-right-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
    border-color: rgb(26, 32, 48);
}

.qp-obj-check {
    width: 14px;
    height: 14px;
    margin-right: 9px;
    border-radius: 3px;
    border-left-width: 1px;
    border-right-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
    border-color: rgb(46, 58, 82);
}

.qp-obj-check--done {
    background-color: rgb(46, 204, 113);
    border-color: rgb(46, 204, 113);
}

.qp-obj-text {
    flex-grow: 1;
    font-size: 12px;
    color: rgb(224, 228, 236);
    white-space: normal;
}

.qp-obj-text--done {
    color: rgb(136, 146, 166);
}

.qp-obj-progress {
    min-width: 30px;
    font-size: 10px;
    -unity-text-align: middle-right;
    color: rgb(136, 146, 166);
}

.qp-obj-progress--full {
    color: rgb(46, 204, 113);
}

/* Progress bar */
.qp-progress-row {
    flex-direction: row;
    align-items: center;
    margin-top: 12px;
}

.qp-progress-track {
    flex-grow: 1;
    height: 3px;
    margin-right: 10px;
    background-color: rgb(8, 10, 16);
    border-radius: 2px;
    overflow: hidden;
}

.qp-progress-fill {
    height: 100%;
    border-radius: 2px;
    background-color: rgb(74, 124, 255);
}

.qp-progress-fill--green {
    background-color: rgb(46, 204, 113);
}

.qp-progress-label {
    min-width: 32px;
    font-size: 9px;
    -unity-text-align: middle-right;
    color: rgb(136, 146, 166);
}

/* Rewards */
.qp-rewards-row {
    flex-direction: row;
    align-items: center;
    flex-wrap: wrap;
    margin-top: 12px;
}

.qp-rewards-label {
    font-size: 9px;
    letter-spacing: 1px;
    margin-right: 8px;
    color: rgb(136, 146, 166);
}

.qp-reward-chip {
    flex-direction: row;
    align-items: center;
    margin-right: 8px;
    margin-bottom: 4px;
    padding-top: 4px;
    padding-bottom: 4px;
    padding-left: 8px;
    padding-right: 8px;
    background-color: rgb(22, 27, 40);
    border-left-width: 1px;
    border-right-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
    border-color: rgb(36, 46, 66);
    border-radius: 5px;
    font-size: 11px;
    color: rgb(224, 228, 236);
}

.qp-reward-dot {
    width: 6px;
    height: 6px;
    margin-right: 5px;
    border-radius: 3px;
    background-color: rgb(212, 160, 23);
}

/* Actions */
.qp-actions {
    flex-direction: row;
    justify-content: flex-end;
    margin-top: 12px;
}

.qp-btn {
    margin-left: 8px;
    padding-top: 7px;
    padding-bottom: 7px;
    padding-left: 18px;
    padding-right: 18px;
    border-radius: 6px;
    font-size: 10px;
    letter-spacing: 1px;
    border-left-width: 1px;
    border-right-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
    border-color: rgba(0, 0, 0, 0);
}

.qp-btn--primary {
    background-color: rgb(74, 124, 255);
    color: rgb(255, 255, 255);
    border-color: rgb(74, 124, 255);
}

.qp-btn--primary:hover {
    background-color: rgb(90, 140, 255);
    border-color: rgb(90, 140, 255);
}

/* Empty state */
.qp-empty {
    align-items: center;
    justify-content: center;
    padding-top: 40px;
    padding-bottom: 40px;
    opacity: 0.4;
}

.qp-empty-icon {
    width: 32px;
    height: 32px;
    margin-bottom: 8px;
    border-radius: 6px;
    border-left-width: 1px;
    border-right-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
    border-color: rgb(46, 58, 82);
}

.qp-empty-text {
    font-size: 10px;
    letter-spacing: 1px;
    color: rgb(136, 146, 166);
}
